{
  "buildCommand": "npm run build",
  "outputDirectory": "front/build",
  "framework": "react-router",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/front/build/$1"
    }
  ]
}
